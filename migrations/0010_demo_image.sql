-- صورة معاينة للتجربة في موقع أثر.
--
-- بطاقةٌ بنصّ وحده لا تُغري بالنقر، والزائر يقرّر في ثانيتين. لكن ثلاثة
-- من أربعة منتجات واجهتُها الأولى شاشةُ دخول — ولقطةٌ لها تُذكّره بالجدار
-- لا بالمنتج. فالحقل اختياري: إن وُضعت صورة (وأفضلها لقطة **بعد** الدخول:
-- لوحة تشغيل أو منيو، لا نموذج تسجيل) عُرضت، وإلا رُسمت معاينة مصمَّمة
-- بهوية المنتج.
ALTER TABLE tenants ADD COLUMN demo_image TEXT NOT NULL DEFAULT '';
